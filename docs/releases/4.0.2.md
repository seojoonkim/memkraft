## MemKraft 4.0.2

### Host-neutral agent bridge

- Added the portable `memkraft bridge call` JSON-stdio contract for agent hosts.
- Added `memkraft setup` integration metadata and `memkraft integrations --json` diagnostics.
- Preserved host-owned credentials, registration, and execution authority boundaries.